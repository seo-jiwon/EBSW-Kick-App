import React from "react";
import Screen from "../Main";

export const AliasChange = ({ navigation}) => <Screen navigation={navigation} name="AliasChange" />;
export const CardInformation = ({ navigation}) => <Screen navigation={navigation} name="CardInformation" />;
